#!/usr/bin/env bash
# This file will make use of the Own-to-Host environment to deploy containers to Host (EDW_DEPLOY_HOST)
# It will also trigger other deploy scripts
# This files assumes that the docker images are built and pushed to docker.hub